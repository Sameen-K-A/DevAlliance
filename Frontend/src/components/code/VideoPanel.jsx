const VideoPanel = ({ videoContainerRef }) => {
   return (
      <div ref={videoContainerRef} className="video-pannel-container position-fixed start-0 bottom-0 m-3 d-flex gap-2">
         
      </div>
   );
};

export default VideoPanel;